# Local, environment-specific config (holds your real Firebase URL) — never commit
config.local.js
.env
.env.*
!.env.example

# Dependencies / build
node_modules/

# OS / editor
.DS_Store

# Tooling
.gstack/

# Large uncompressed source video — commit the compressed
# DataSafeguard-webflow_540.mp4 (~1.7MB) instead, which is what the widget loads.
DataSafeguard-webflow_1080p.mp4
