# Local, environment-specific config (holds your real Firebase URL) — never commit
config.local.js
.env
.env.*
!.env.example

# Dependencies / build
node_modules/

# OS / editor
.DS_Store

# Tooling
.gstack/

# Videos: the uncompressed master, and any root-level copy of the 540p, stay out
# of git. The ONLY tracked video is cdn-deploy/DataSafeguard-webflow_540.mp4.
DataSafeguard-webflow_1080p.mp4
/DataSafeguard-webflow_540.mp4

# NOTE: cdn-deploy/ is the tracked build output (run ./build.sh to refresh it).
